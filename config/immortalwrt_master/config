# ================================

# FULL FEATURE SET (OPTIMAL HG680P)

# ================================

# --- STORAGE & FILESYSTEM ---

CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_kmod-nls-base=y
CONFIG_PACKAGE_kmod-nls-utf8=y

# --- NETWORK DEBUG & TOOLS ---

CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_nano=y
CONFIG_PACKAGE_ethtool=y
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_bind-dig=y

# --- DNS (RINGAN & STABIL) ---

CONFIG_PACKAGE_https-dns-proxy=y

# CONFIG_PACKAGE_adguardhome is not set

# --- SQM (ANTI BUFFERBLOAT) ---

CONFIG_PACKAGE_luci-app-sqm=y
CONFIG_PACKAGE_sqm-scripts=y

# --- MONITORING ---

CONFIG_PACKAGE_luci-app-nlbwmon=y
CONFIG_PACKAGE_luci-app-statistics=y
CONFIG_PACKAGE_collectd=y
CONFIG_PACKAGE_collectd-mod-cpu=y
CONFIG_PACKAGE_collectd-mod-interface=y
CONFIG_PACKAGE_collectd-mod-memory=y
CONFIG_PACKAGE_collectd-mod-load=y

# --- SYSTEM & MAINTENANCE ---

CONFIG_PACKAGE_luci-app-commands=y
CONFIG_PACKAGE_luci-app-attendedsysupgrade=y
CONFIG_PACKAGE_luci-app-reboot=y
CONFIG_PACKAGE_logd=y

# --- TIME SYNC ---

CONFIG_PACKAGE_ntpclient=y

# --- CPU SCALING ---

CONFIG_PACKAGE_kmod-cpufreq=y

# --- FIREWALL EXTRA ---

CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-app-upnp=y

# --- FILE SHARING (OPSIONAL NAS) ---

CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-ntfs=y
CONFIG_PACKAGE_ntfs-3g=y
CONFIG_PACKAGE_kmod-fs-exfat=y

# --- SAMBA (SHARE KE WINDOWS) ---

CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_samba4-server=y

# --- SSH FILE TRANSFER (SUDAH ADA, TAMBAHAN TOOL) ---

CONFIG_PACKAGE_openssh-sftp-server=y

# ================================

# TETAP NONAKTIFKAN (PENTING)

# ================================

# --- APK (JANGAN ADA) ---

# CONFIG_PACKAGE_apk is not set

# CONFIG_PACKAGE_apk-mbedtls is not set

# CONFIG_PACKAGE_apk-openssl is not set

# --- WIFI INTERNAL (MATIKAN) ---

# CONFIG_PACKAGE_kmod-brcmfmac is not set

# CONFIG_PACKAGE_kmod-rtl8xxxu is not set

# CONFIG_PACKAGE_kmod-rtl8192cu is not set

# --- PAKET BERAT (JANGAN AKTIFKAN) ---

# CONFIG_PACKAGE_docker is not set

# CONFIG_PACKAGE_containerd is not set

# CONFIG_PACKAGE_k3s is not set

# CONFIG_PACKAGE_lxc is not set

# --- FRP (MAKAN RAM) ---

# CONFIG_PACKAGE_frpc is not set

# CONFIG_PACKAGE_frps is not set
